# This is a simple exercise to allow users to input bio info

first = input("First Name: ")  # prompt that requests users first name
last = input("Last Name: ") # prompt that requests users last name
print("Welcome", first, last + "!")  # prints the users first and last name with an exclamation point after the last name without a space
